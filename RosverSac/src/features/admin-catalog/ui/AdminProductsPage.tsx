/** Módulo Productos — vacío; CRUD y API en siguientes iteraciones. */
export function AdminProductsPage() {
  return (
    <section className="flex min-h-[60vh] flex-col items-center justify-center rounded-3xl bg-white px-6 py-16 text-center shadow-sm">
      <p className="text-[11px] font-bold tracking-[0.18em] text-rosver-muted uppercase">
        Módulo
      </p>
      <h1 className="mt-2 font-display text-2xl font-bold text-rosver-ink sm:text-3xl">
        Productos
      </h1>
      <p className="mt-3 max-w-md text-sm text-rosver-muted">
        Vista lista para el CRUD de catálogo (SKU, precios, visibilidad web,
        imágenes R2). Por ahora está vacía a propósito.
      </p>
    </section>
  )
}
