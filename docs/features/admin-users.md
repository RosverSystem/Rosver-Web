# Feature: Admin — usuarios

**Slug:** `features/admin-users/`  
**Estado:** activa (wireframe de baja fidelidad; diseño final pendiente)

## Propósito

Gestionar usuarios y roles (`client`, `sales`, `admin`). Solo admin.

## Verificación

- [x] Listado + asignación de rol (UI) — `AdminUsersPage`, `/admin/usuarios`
- [ ] Restringir acceso solo a `admin` (fase lógica)
- [ ] Reemplazar `WireBlock` por diseño final
